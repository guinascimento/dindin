%html
	%head	
		%title
			Dindin
		=stylesheet_link_tag "scaffold", "jquery-ui"
		%script{:type => "text/javascript", :src => "/javascripts/jquery.js"}
		%script{:type => "text/javascript", :src => "/javascripts/jqueryui.js"}
		%script{:type => "text/javascript", :src => "/javascripts/jquery.maskMoney.js"}

		%script{:type => "text/javascript"}
			:plain
				$(function() {
					$("#datepicker").datepicker({ dateFormat: 'dd-mm-yy' });
					$("#ammount").maskMoney({decimal:",", thousands:"."});
				});

	%body{:onload => "maska()"}
		#header
			#site-title
				Dindin
			#site-subtitle
				Keep track of your money!
			#navigation
				=link_to "Dashboard", { :controller => "dashboard", :action => "index" }, :class => is_active?("invoices")
				%span{:class => "separator"}
					|
				=link_to "Reports", "/#{params[:month]}/reports", :class => is_active?("report")
		=yield
